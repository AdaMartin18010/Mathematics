# 迁移工具说明 - Mathematics项目

**创建日期**: 2025-12-20
**版本**: v1.0
**状态**: 可用
**目的**: 提供迁移过程中使用的工具和脚本说明

---

## 📋 工具概述

本文档说明在内容迁移过程中可以使用的工具和脚本，帮助自动化迁移过程，提高效率并降低错误率。

---

## 🛠️ 可用工具

### 1. PowerShell脚本工具

#### 1.1 备份脚本

**功能**: 创建完整备份

**脚本示例**:

```powershell
# 创建备份目录
$backupDir = "..\Mathematics_Backup_2025-12-20"
New-Item -ItemType Directory -Path $backupDir -Force

# 复制整个Math目录
Copy-Item -Path "..\Math" -Destination "$backupDir\Math" -Recurse -Force

# 验证备份
$sourceCount = (Get-ChildItem -Path "..\Math" -Recurse -File | Measure-Object).Count
$backupCount = (Get-ChildItem -Path "$backupDir\Math" -Recurse -File | Measure-Object).Count

Write-Host "源文件数: $sourceCount"
Write-Host "备份文件数: $backupCount"

if ($sourceCount -eq $backupCount) {
    Write-Host "✅ 备份验证成功" -ForegroundColor Green
} else {
    Write-Host "⚠️ 备份验证失败" -ForegroundColor Red
}
```

**使用方法**:

1. 打开PowerShell
2. 导航到项目根目录
3. 运行脚本

---

#### 1.2 目录移动脚本

**功能**: 移动目录到新位置

**脚本示例**:

```powershell
# 移动Analysis模块
$source = "..\Math\Analysis"
$destination = "Mathematics\01-核心内容\Analysis"

if (Test-Path $source) {
    # 确保目标目录的父目录存在
    $parentDir = Split-Path $destination -Parent
    if (-not (Test-Path $parentDir)) {
        New-Item -ItemType Directory -Path $parentDir -Force
    }

    # 移动目录
    Move-Item -Path $source -Destination $destination -Force
    Write-Host "✅ Analysis模块迁移完成" -ForegroundColor Green
} else {
    Write-Host "⚠️ 源目录不存在: $source" -ForegroundColor Yellow
}
```

**使用方法**:

1. 修改 `$source` 和 `$destination` 变量
2. 运行脚本

---

#### 1.3 文件统计脚本

**功能**: 统计文件数量

**脚本示例**:

```powershell
# 统计Analysis模块文件数
$modulePath = "..\Math\Analysis"
$fileCount = (Get-ChildItem -Path $modulePath -Recurse -File | Measure-Object).Count
$dirCount = (Get-ChildItem -Path $modulePath -Recurse -Directory | Measure-Object).Count

Write-Host "模块路径: $modulePath"
Write-Host "文件数: $fileCount"
Write-Host "目录数: $dirCount"
```

---

### 2. 批量迁移脚本

**功能**: 批量迁移多个模块

**脚本示例**:

```powershell
# 定义迁移映射
$migrationMap = @{
    "Analysis" = @{
        Source = "..\Math\Analysis"
        Destination = "Mathematics\01-核心内容\Analysis"
    }
    "Refactor" = @{
        Source = "..\Math\Refactor"
        Destination = "Mathematics\01-核心内容\Refactor"
    }
    "Lean" = @{
        Source = "..\Math\Lean"
        Destination = "Mathematics\01-核心内容\Lean"
    }
}

# 执行迁移
foreach ($module in $migrationMap.Keys) {
    $config = $migrationMap[$module]
    $source = $config.Source
    $destination = $config.Destination

    Write-Host "正在迁移: $module" -ForegroundColor Cyan

    if (Test-Path $source) {
        # 确保目标父目录存在
        $parentDir = Split-Path $destination -Parent
        if (-not (Test-Path $parentDir)) {
            New-Item -ItemType Directory -Path $parentDir -Force
        }

        # 移动目录
        Move-Item -Path $source -Destination $destination -Force
        Write-Host "✅ $module 迁移完成" -ForegroundColor Green
    } else {
        Write-Host "⚠️ 源目录不存在: $source" -ForegroundColor Yellow
    }
}
```

---

### 3. 链接检查工具

**功能**: 检查Markdown文件中的链接有效性

**说明**: 可以使用工具如 `markdown-link-check` 或编写自定义脚本

**建议工具**:

- [markdown-link-check](https://github.com/tcort/markdown-link-check)
- 自定义PowerShell脚本

---

### 4. 文件验证脚本

**功能**: 验证迁移后文件的完整性

**脚本示例**:

```powershell
# 验证Analysis模块迁移
$sourcePath = "..\Math\Analysis"
$targetPath = "Mathematics\01-核心内容\Analysis"

if (Test-Path $targetPath) {
    $sourceFiles = Get-ChildItem -Path $sourcePath -Recurse -File | Select-Object -ExpandProperty Name
    $targetFiles = Get-ChildItem -Path $targetPath -Recurse -File | Select-Object -ExpandProperty Name

    $missingFiles = $sourceFiles | Where-Object { $_ -notin $targetFiles }

    if ($missingFiles.Count -eq 0) {
        Write-Host "✅ 所有文件已成功迁移" -ForegroundColor Green
    } else {
        Write-Host "⚠️ 以下文件缺失:" -ForegroundColor Yellow
        $missingFiles | ForEach-Object { Write-Host "  - $_" }
    }
} else {
    Write-Host "⚠️ 目标目录不存在" -ForegroundColor Yellow
}
```

---

## 📝 使用建议

### 迁移前

1. **创建备份**: 使用备份脚本创建完整备份
2. **验证备份**: 确认备份完整性
3. **检查空间**: 确保有足够的磁盘空间

### 迁移中

1. **分模块迁移**: 逐个模块迁移，不要一次性迁移所有内容
2. **验证每一步**: 每个模块迁移后验证
3. **记录过程**: 记录迁移过程中的任何问题

### 迁移后

1. **验证完整性**: 使用验证脚本检查文件完整性
2. **检查链接**: 使用链接检查工具验证链接
3. **更新文档**: 更新相关文档和索引

---

## ⚠️ 注意事项

1. **权限要求**: 确保有足够的文件系统权限
2. **路径长度**: Windows系统可能有路径长度限制（260字符）
3. **备份优先**: 始终先创建备份再执行迁移
4. **测试脚本**: 在测试环境中先测试脚本

---

## 🔗 相关文档

- [迁移执行指南](./迁移执行指南.md)
- [迁移执行记录](./迁移执行记录.md)
- [内容迁移规划](./内容迁移规划.md)

---

**最后更新**: 2025-12-20
**维护**: Mathematics项目团队
**版本**: v1.0
