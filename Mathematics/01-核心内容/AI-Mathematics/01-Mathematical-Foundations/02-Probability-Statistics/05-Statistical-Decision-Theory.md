# 统计决策理论 (Statistical Decision Theory)

> **The Mathematical Foundation of Optimal Decision Making**
>
> 最优决策的数学基础

---

## 目录

- [统计决策理论 (Statistical Decision Theory)](#统计决策理论-statistical-decision-theory)
  - [目录](#目录)
  - [📋 核心思想](#-核心思想)
  - [1. 决策问题框架](#1-决策问题框架)
    - [1.1 基本要素](#11-基本要素)
    - [1.2 决策函数](#12-决策函数)
    - [1.3 损失函数](#13-损失函数)
  - [2. 风险函数与最优性准则](#2-风险函数与最优性准则)
    - [2.1 风险函数](#21-风险函数)
    - [2.2 最优性准则](#22-最优性准则)
      - [最小最大准则](#最小最大准则)
      - [贝叶斯准则](#贝叶斯准则)
      - [可容许性](#可容许性)
  - [3. 贝叶斯决策理论](#3-贝叶斯决策理论)
    - [3.1 先验分布](#31-先验分布)
    - [3.2 后验风险](#32-后验风险)
    - [3.3 贝叶斯最优决策](#33-贝叶斯最优决策)
  - [4. 经典决策理论](#4-经典决策理论)
    - [4.1 无偏估计](#41-无偏估计)
    - [4.2 最小方差无偏估计 (UMVUE)](#42-最小方差无偏估计-umvue)
    - [4.3 Cramér-Rao下界](#43-cramér-rao下界)
  - [5. 机器学习中的应用](#5-机器学习中的应用)
    - [5.1 监督学习框架](#51-监督学习框架)
    - [5.2 经验风险最小化](#52-经验风险最小化)
    - [5.3 结构风险最小化](#53-结构风险最小化)
    - [5.4 正则化理论](#54-正则化理论)
  - [6. 形式化定义 (Lean)](#6-形式化定义-lean)
  - [7. 习题](#7-习题)
  - [8. 参考资料](#8-参考资料)

---

## 📋 核心思想

**统计决策理论**为在不确定性下做出最优决策提供了数学框架。

**为什么统计决策理论重要**:

```text
理论意义:
├─ 统一了估计、检验和预测问题
├─ 提供了最优性准则
└─ 连接了频率派和贝叶斯派观点

机器学习应用:
├─ 损失函数设计 → 决策理论
├─ 正则化 → 贝叶斯先验
├─ 模型选择 → 风险最小化
└─ 泛化理论 → 结构风险最小化
```

---

## 1. 决策问题框架

### 1.1 基本要素

**定义 1.1** (统计决策问题)
一个统计决策问题由以下要素组成：

1. **参数空间** \( \Theta \): 未知参数 \( \theta \) 的所有可能取值
2. **样本空间** \( \mathcal{X} \): 观测数据 \( X \) 的所有可能取值
3. **行动空间** \( \mathcal{A} \): 决策者可以采取的所有行动 \( a \)
4. **损失函数** \( L: \Theta \times \mathcal{A} \to \mathbb{R} \): 参数为 \( \theta \) 时采取行动 \( a \) 的损失
5. **决策函数** \( \delta: \mathcal{X} \to \mathcal{A} \): 根据观测数据 \( X \) 选择行动 \( a = \delta(X) \)

**例 1.1** (点估计问题)
- 参数空间: \( \Theta = \mathbb{R} \) (估计均值 \( \mu \))
- 样本空间: \( \mathcal{X} = \mathbb{R}^n \) (n个观测值)
- 行动空间: \( \mathcal{A} = \mathbb{R} \) (估计值)
- 损失函数: \( L(\theta, a) = (\theta - a)^2 \) (平方损失)

**例 1.2** (假设检验问题)
- 参数空间: \( \Theta = \{\theta_0, \theta_1\} \) (零假设 vs 备择假设)
- 样本空间: \( \mathcal{X} = \mathbb{R}^n \)
- 行动空间: \( \mathcal{A} = \{0, 1\} \) (接受/拒绝零假设)
- 损失函数:
  \[
  L(\theta, a) = \begin{cases}
  0 & \text{如果决策正确} \\
  c_0 & \text{如果 } \theta = \theta_0 \text{ 但 } a = 1 \text{ (第一类错误)} \\
  c_1 & \text{如果 } \theta = \theta_1 \text{ 但 } a = 0 \text{ (第二类错误)}
  \end{cases}
  \]

### 1.2 决策函数

**定义 1.2** (决策函数)
决策函数 \( \delta: \mathcal{X} \to \mathcal{A} \) 是一个从样本空间到行动空间的映射，表示根据观测数据选择行动的策略。

**例 1.3** (样本均值估计)
对于估计均值问题，决策函数可以是：
\[
\delta(X) = \bar{X} = \frac{1}{n}\sum_{i=1}^n X_i
\]

### 1.3 损失函数

**定义 1.3** (损失函数)
损失函数 \( L(\theta, a) \) 衡量当真实参数为 \( \theta \) 时，采取行动 \( a \) 的"成本"。

**常见损失函数**:

1. **平方损失**: \( L(\theta, a) = (\theta - a)^2 \)
2. **绝对损失**: \( L(\theta, a) = |\theta - a| \)
3. **0-1损失**:
   \[
   L(\theta, a) = \begin{cases}
   0 & \text{如果 } a = \theta \\
   1 & \text{如果 } a \neq \theta
   \end{cases}
   \]
4. **Huber损失**:
   \[
   L(\theta, a) = \begin{cases}
   \frac{1}{2}(\theta - a)^2 & \text{如果 } |\theta - a| \leq \delta \\
   \delta|\theta - a| - \frac{\delta^2}{2} & \text{如果 } |\theta - a| > \delta
   \end{cases}
   \]

---

## 2. 风险函数与最优性准则

### 2.1 风险函数

**定义 2.1** (风险函数)
决策函数 \( \delta \) 在参数 \( \theta \) 下的**风险函数**定义为：
\[
R(\theta, \delta) = \mathbb{E}_\theta[L(\theta, \delta(X))]
\]

即损失函数关于数据分布的期望。

**例 2.1** (平方损失下的风险)
对于平方损失 \( L(\theta, a) = (\theta - a)^2 \)，风险函数为：
\[
R(\theta, \delta) = \mathbb{E}_\theta[(\theta - \delta(X))^2] = \text{MSE}(\delta) = \text{Bias}^2(\delta) + \text{Var}(\delta)
\]

其中：
- **偏差**: \( \text{Bias}(\delta) = \mathbb{E}_\theta[\delta(X)] - \theta \)
- **方差**: \( \text{Var}(\delta) = \mathbb{E}_\theta[(\delta(X) - \mathbb{E}_\theta[\delta(X)])^2] \)

### 2.2 最优性准则

由于风险函数依赖于未知参数 \( \theta \)，我们需要定义"最优"的含义。

#### 最小最大准则

**定义 2.2** (最小最大决策函数)
决策函数 \( \delta^* \) 称为**最小最大**的，如果：
\[
\sup_{\theta \in \Theta} R(\theta, \delta^*) = \inf_{\delta} \sup_{\theta \in \Theta} R(\theta, \delta)
\]

即最小化最坏情况下的风险。

**例 2.2** (最小最大估计)
在某些情况下，样本中位数是最小最大估计（对于绝对损失）。

#### 贝叶斯准则

**定义 2.3** (贝叶斯风险)
给定先验分布 \( \pi(\theta) \)，决策函数 \( \delta \) 的**贝叶斯风险**为：
\[
r(\pi, \delta) = \int_\Theta R(\theta, \delta) \pi(\theta) d\theta = \mathbb{E}_\pi[R(\theta, \delta)]
\]

**定义 2.4** (贝叶斯决策函数)
决策函数 \( \delta^* \) 称为**贝叶斯最优**的，如果：
\[
r(\pi, \delta^*) = \inf_{\delta} r(\pi, \delta)
\]

#### 可容许性

**定义 2.5** (可容许决策函数)
决策函数 \( \delta \) 称为**可容许**的，如果不存在另一个决策函数 \( \delta' \) 使得：
- \( R(\theta, \delta') \leq R(\theta, \delta) \) 对所有 \( \theta \in \Theta \) 成立
- 且至少对一个 \( \theta \) 有严格不等式

即不存在"处处更好"的决策函数。

---

## 3. 贝叶斯决策理论

### 3.1 先验分布

**定义 3.1** (先验分布)
先验分布 \( \pi(\theta) \) 表示在观测数据之前对参数 \( \theta \) 的信念。

**常见先验**:

1. **共轭先验**: 使得后验分布与先验分布属于同一分布族
2. **无信息先验**: 表示对参数没有先验知识
3. **Jeffreys先验**: 基于Fisher信息矩阵的无信息先验

### 3.2 后验风险

**定义 3.2** (后验分布)
给定观测数据 \( X = x \)，参数 \( \theta \) 的**后验分布**为：
\[
\pi(\theta | x) = \frac{f(x | \theta) \pi(\theta)}{m(x)}
\]

其中 \( m(x) = \int_\Theta f(x | \theta) \pi(\theta) d\theta \) 是边际分布。

**定义 3.3** (后验风险)
给定观测数据 \( X = x \)，行动 \( a \) 的**后验风险**为：
\[
\rho(\pi(\cdot | x), a) = \int_\Theta L(\theta, a) \pi(\theta | x) d\theta
\]

### 3.3 贝叶斯最优决策

**定理 3.1** (贝叶斯最优决策)
对于平方损失 \( L(\theta, a) = (\theta - a)^2 \)，贝叶斯最优决策为后验均值：
\[
\delta^*(x) = \mathbb{E}[\theta | X = x] = \int_\Theta \theta \pi(\theta | x) d\theta
\]

**证明**:
后验风险为：
\[
\rho(\pi(\cdot | x), a) = \int_\Theta (\theta - a)^2 \pi(\theta | x) d\theta
\]

对 \( a \) 求导并令其为零：
\[
\frac{d}{da} \rho(\pi(\cdot | x), a) = -2 \int_\Theta (\theta - a) \pi(\theta | x) d\theta = 0
\]

因此：
\[
a = \int_\Theta \theta \pi(\theta | x) d\theta = \mathbb{E}[\theta | X = x]
\]

**定理 3.2** (绝对损失下的贝叶斯最优决策)
对于绝对损失 \( L(\theta, a) = |\theta - a| \)，贝叶斯最优决策为后验中位数。

---

## 4. 经典决策理论

### 4.1 无偏估计

**定义 4.1** (无偏估计)
估计量 \( \delta(X) \) 称为**无偏**的，如果：
\[
\mathbb{E}_\theta[\delta(X)] = \theta \quad \text{对所有 } \theta \in \Theta
\]

**例 4.1** (样本均值)
对于 \( X_1, \ldots, X_n \sim \mathcal{N}(\mu, \sigma^2) \)，样本均值 \( \bar{X} \) 是 \( \mu \) 的无偏估计。

### 4.2 最小方差无偏估计 (UMVUE)

**定义 4.2** (UMVUE)
无偏估计量 \( \delta^* \) 称为**最小方差无偏估计 (UMVUE)**，如果对任何其他无偏估计量 \( \delta \)：
\[
\text{Var}_\theta(\delta^*(X)) \leq \text{Var}_\theta(\delta(X)) \quad \text{对所有 } \theta \in \Theta
\]

**定理 4.1** (Lehmann-Scheffé定理)
如果 \( T \) 是充分且完备统计量，且 \( \delta(T) \) 是无偏估计，则 \( \delta(T) \) 是UMVUE。

### 4.3 Cramér-Rao下界

**定理 4.2** (Cramér-Rao不等式)
对于正则分布族，任何无偏估计量 \( \delta(X) \) 的方差满足：
\[
\text{Var}_\theta(\delta(X)) \geq \frac{1}{I(\theta)}
\]

其中 \( I(\theta) \) 是Fisher信息量：
\[
I(\theta) = \mathbb{E}_\theta\left[\left(\frac{\partial \log f(X | \theta)}{\partial \theta}\right)^2\right]
\]

**定义 4.3** (有效估计)
如果无偏估计量达到Cramér-Rao下界，则称为**有效估计**。

---

## 5. 机器学习中的应用

### 5.1 监督学习框架

**监督学习作为决策问题**:

- **参数空间**: 假设空间 \( \mathcal{H} \) (所有可能的模型)
- **样本空间**: 训练数据 \( \{(x_i, y_i)\}_{i=1}^n \)
- **行动空间**: 预测函数 \( f: \mathcal{X} \to \mathcal{Y} \)
- **损失函数**: 预测损失 \( L(y, f(x)) \)

**例 5.1** (分类问题)
- 损失函数: 0-1损失 \( L(y, \hat{y}) = \mathbb{1}\{y \neq \hat{y}\} \)
- 风险函数: 分类错误率

**例 5.2** (回归问题)
- 损失函数: 平方损失 \( L(y, \hat{y}) = (y - \hat{y})^2 \)
- 风险函数: 均方误差 (MSE)

### 5.2 经验风险最小化

**定义 5.1** (经验风险)
给定训练数据 \( \{(x_i, y_i)\}_{i=1}^n \)，模型 \( f \) 的**经验风险**为：
\[
R_{\text{emp}}(f) = \frac{1}{n}\sum_{i=1}^n L(y_i, f(x_i))
\]

**定义 5.2** (经验风险最小化, ERM)
**经验风险最小化**选择：
\[
\hat{f} = \arg\min_{f \in \mathcal{H}} R_{\text{emp}}(f)
\]

**问题**: ERM可能导致过拟合，特别是当假设空间 \( \mathcal{H} \) 很大时。

### 5.3 结构风险最小化

**定义 5.3** (结构风险最小化, SRM)
**结构风险最小化**选择：
\[
\hat{f} = \arg\min_{f \in \mathcal{H}} \left[ R_{\text{emp}}(f) + \lambda \Omega(f) \right]
\]

其中：
- \( R_{\text{emp}}(f) \): 经验风险（拟合数据）
- \( \Omega(f) \): 复杂度惩罚项（控制模型复杂度）
- \( \lambda \): 正则化参数（平衡拟合与复杂度）

**例 5.3** (L2正则化)
对于线性模型 \( f(x) = w^T x \)，L2正则化对应：
\[
\Omega(f) = \|w\|_2^2
\]

这等价于假设权重 \( w \) 的先验分布为 \( \mathcal{N}(0, \lambda^{-1} I) \)。

### 5.4 正则化理论

**定理 5.1** (Tikhonov正则化)
对于平方损失，L2正则化的解为：
\[
\hat{w} = (X^T X + \lambda I)^{-1} X^T y
\]

这可以解释为：
1. **频率派观点**: 在经验风险和模型复杂度之间权衡
2. **贝叶斯观点**: 最大后验估计 (MAP)，其中先验为 \( w \sim \mathcal{N}(0, \lambda^{-1} I) \)

**定理 5.2** (泛化误差界)
对于VC维为 \( d \) 的假设空间，以概率至少 \( 1 - \delta \)：
\[
R(f) \leq R_{\text{emp}}(f) + O\left(\sqrt{\frac{d \log(n/d) + \log(1/\delta)}{n}}\right)
\]

这提供了经验风险和真实风险之间的理论联系。

---

## 6. 形式化定义 (Lean)

```lean
-- 决策问题框架
structure DecisionProblem (Θ : Type*) (𝒳 : Type*) (𝒜 : Type*) where
  loss : Θ → 𝒜 → ℝ
  decision : 𝒳 → 𝒜

-- 风险函数
def risk {Θ 𝒳 𝒜 : Type*} [MeasurableSpace Θ] [MeasurableSpace 𝒳]
  (θ : Θ) (δ : 𝒳 → 𝒜) (loss : Θ → 𝒜 → ℝ) (P : Measure 𝒳) : ℝ :=
  ∫ x, loss θ (δ x) ∂P

-- 贝叶斯风险
def bayes_risk {Θ 𝒳 𝒜 : Type*} [MeasurableSpace Θ] [MeasurableSpace 𝒳]
  (π : Measure Θ) (δ : 𝒳 → 𝒜) (loss : Θ → 𝒜 → ℝ) : ℝ :=
  ∫ θ, risk θ δ loss (conditional_distribution θ) ∂π
```

---

## 7. 习题

### 基础习题

1. **风险函数计算**:
   对于 \( X_1, \ldots, X_n \sim \mathcal{N}(\mu, 1) \)，计算估计量 \( \delta(X) = \bar{X} \) 在平方损失下的风险函数。

2. **贝叶斯估计**:
   对于 \( X \sim \text{Binomial}(n, \theta) \)，假设先验 \( \theta \sim \text{Beta}(\alpha, \beta) \)，求平方损失下的贝叶斯估计。

3. **Cramér-Rao下界**:
   对于 \( X \sim \mathcal{N}(\mu, \sigma^2) \)（\( \sigma^2 \) 已知），验证样本均值达到Cramér-Rao下界。

### 进阶习题

1. **最小最大估计**:
   证明对于绝对损失，样本中位数在某些情况下是最小最大估计。

2. **正则化与贝叶斯**:
   证明L2正则化等价于高斯先验下的MAP估计。

3. **泛化误差**:
   使用VC理论推导分类问题的泛化误差界。

---

## 8. 参考资料

### 教材

1. **Berger, J. O.** *Statistical Decision Theory and Bayesian Analysis*. Springer, 1985.
2. **Lehmann, E. L. & Casella, G.** *Theory of Point Estimation*. Springer, 1998.
3. **Wasserman, L.** *All of Statistics*. Springer, 2004.

### 课程

1. **MIT 18.443** - Statistics for Applications
2. **Stanford STATS 315A** - Modern Applied Statistics

### 论文

1. **Vapnik, V. N.** *Statistical Learning Theory*. Wiley, 1998.
2. **Bousquet, O. et al.** "Introduction to Statistical Learning Theory." *Advanced Lectures on Machine Learning*, 2004.

---

**最后更新**: 2025-12-20
**完成度**: 约85% (核心内容完成，可继续补充更多应用实例和形式化证明)
