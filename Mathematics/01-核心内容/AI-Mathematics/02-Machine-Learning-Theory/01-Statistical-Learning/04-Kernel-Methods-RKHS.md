# 核方法与再生核Hilbert空间 (Kernel Methods and RKHS)

> **From Linear to Non-linear: The Power of Kernels**
>
> 从线性到非线性：核方法的力量

---

## 目录

- [核方法与再生核Hilbert空间 (Kernel Methods and RKHS)](#核方法与再生核hilbert空间-kernel-methods-and-rkhs)
  - [目录](#目录)
  - [📋 核心思想](#-核心思想)
  - [1. 核函数基础](#1-核函数基础)
    - [1.1 核函数定义](#11-核函数定义)
    - [1.2 正定核](#12-正定核)
    - [1.3 常用核函数](#13-常用核函数)
  - [2. 特征映射与核技巧](#2-特征映射与核技巧)
    - [2.1 特征映射](#21-特征映射)
    - [2.2 核技巧](#22-核技巧)
    - [2.3 计算优势](#23-计算优势)
  - [3. 再生核Hilbert空间 (RKHS)](#3-再生核hilbert空间-rkhs)
    - [3.1 RKHS定义](#31-rkhs定义)
    - [3.2 再生性质](#32-再生性质)
    - [3.3 表示定理](#33-表示定理)
  - [4. Representer定理](#4-representer定理)
    - [4.1 Representer定理](#41-representer定理)
    - [4.2 应用](#42-应用)
  - [5. 支持向量机 (SVM)](#5-支持向量机-svm)
    - [5.1 线性SVM](#51-线性svm)
    - [5.2 核SVM](#52-核svm)
    - [5.3 对偶问题](#53-对偶问题)
  - [6. 核岭回归](#6-核岭回归)
    - [6.1 核岭回归](#61-核岭回归)
    - [6.2 正则化路径](#62-正则化路径)
  - [7. 高斯过程](#7-高斯过程)
    - [7.1 高斯过程定义](#71-高斯过程定义)
    - [7.2 核函数选择](#72-核函数选择)
    - [7.3 预测与不确定性](#73-预测与不确定性)
  - [8. 核方法在深度学习中的应用](#8-核方法在深度学习中的应用)
    - [8.1 神经切线核 (NTK)](#81-神经切线核-ntk)
    - [8.2 核方法与深度学习的联系](#82-核方法与深度学习的联系)
  - [9. 形式化定义 (Lean)](#9-形式化定义-lean)
  - [10. 习题](#10-习题)
    - [基础习题](#基础习题)
    - [进阶习题](#进阶习题)
  - [11. 参考资料](#11-参考资料)
    - [教材](#教材)
    - [课程](#课程)
    - [论文](#论文)

---

## 📋 核心思想

**核方法**通过将数据映射到高维特征空间，使线性算法能够处理非线性问题。

**为什么核方法重要**:

```text
核心问题:
├─ 如何用线性算法处理非线性数据？
├─ 如何避免显式计算高维特征？
├─ 如何理解特征空间的几何结构？
└─ 核方法与深度学习的关系？

理论工具:
├─ 正定核: 保证特征空间存在
├─ RKHS: 提供理论框架
├─ Representer定理: 简化优化
└─ 核技巧: 避免高维计算

实践应用:
├─ SVM: 分类与回归
├─ 核岭回归: 非线性回归
├─ 高斯过程: 贝叶斯非参数方法
└─ 神经切线核: 理解深度学习
```

---

## 1. 核函数基础

### 1.1 核函数定义

**定义 1.1** (核函数)
设 \( \mathcal{X} \) 是输入空间，函数 \( k: \mathcal{X} \times \mathcal{X} \to \mathbb{R} \) 称为**核函数**，如果存在特征映射 \( \phi: \mathcal{X} \to \mathcal{H} \) 使得：
\[
k(x, x') = \langle \phi(x), \phi(x') \rangle_{\mathcal{H}}
\]

其中 \( \mathcal{H} \) 是Hilbert空间（特征空间）。

### 1.2 正定核

**定义 1.2** (正定核)
核函数 \( k \) 称为**正定核**（或Mercer核），如果对任意 \( n \in \mathbb{N} \) 和任意 \( x_1, \ldots, x_n \in \mathcal{X} \)，Gram矩阵：
\[
K_{ij} = k(x_i, x_j)
\]
是半正定的。

**定理 1.1** (Mercer定理)
如果 \( k \) 是正定核，则存在特征映射 \( \phi \) 使得 \( k(x, x') = \langle \phi(x), \phi(x') \rangle \)。

### 1.3 常用核函数

**线性核**:
\[
k(x, x') = \langle x, x' \rangle
\]

**多项式核**:
\[
k(x, x') = (\langle x, x' \rangle + c)^d
\]

**RBF核（高斯核）**:
\[
k(x, x') = \exp\left(-\frac{\|x - x'\|^2}{2\sigma^2}\right)
\]

**Sigmoid核**:
\[
k(x, x') = \tanh(\alpha \langle x, x' \rangle + c)
\]

---

## 2. 特征映射与核技巧

### 2.1 特征映射

**定义 2.1** (特征映射)
**特征映射** \( \phi: \mathcal{X} \to \mathcal{H} \) 将输入空间映射到高维（可能无限维）特征空间。

**例 2.1** (多项式特征)
对于 \( x \in \mathbb{R}^2 \)，二次多项式特征映射为：
\[
\phi(x) = (x_1^2, \sqrt{2}x_1 x_2, x_2^2)
\]

对应的核函数为：
\[
k(x, x') = \langle \phi(x), \phi(x') \rangle = (\langle x, x' \rangle)^2
\]

### 2.2 核技巧

**核技巧**的核心思想：在算法中只使用 \( k(x, x') \) 而不显式计算 \( \phi(x) \)。

**优势**:

- 避免高维计算
- 可以使用无限维特征空间
- 计算复杂度与输入维度无关

### 2.3 计算优势

**例 2.2** (RBF核的特征空间)
RBF核对应无限维特征空间，但计算 \( k(x, x') \) 只需要 \( O(d) \) 时间（\( d \) 是输入维度）。

---

## 3. 再生核Hilbert空间 (RKHS)

### 3.1 RKHS定义

**定义 3.1** (RKHS)
设 \( \mathcal{H} \) 是定义在 \( \mathcal{X} \) 上的函数空间，如果存在核函数 \( k: \mathcal{X} \times \mathcal{X} \to \mathbb{R} \) 使得：

1. 对任意 \( x \in \mathcal{X} \)，\( k(\cdot, x) \in \mathcal{H} \)
2. 对任意 \( f \in \mathcal{H} \) 和 \( x \in \mathcal{X} \)，\( f(x) = \langle f, k(\cdot, x) \rangle_{\mathcal{H}} \)

则 \( \mathcal{H} \) 称为**再生核Hilbert空间 (RKHS)**，\( k \) 称为**再生核**。

### 3.2 再生性质

**定理 3.1** (再生性质)
在RKHS中，对任意 \( f \in \mathcal{H} \) 和 \( x \in \mathcal{X} \)：
\[
f(x) = \langle f, k(\cdot, x) \rangle_{\mathcal{H}}
\]

**解释**: 函数在点 \( x \) 的值等于函数与核函数 \( k(\cdot, x) \) 的内积。

### 3.3 表示定理

**定理 3.2** (RKHS表示定理)
RKHS中的任意函数 \( f \) 可以表示为：
\[
f = \sum_{i=1}^n \alpha_i k(\cdot, x_i)
\]

其中 \( \{x_i\} \) 是训练样本。

---

## 4. Representer定理

### 4.1 Representer定理

**定理 4.1** (Representer定理)
考虑优化问题：
\[
\min_{f \in \mathcal{H}} \sum_{i=1}^n L(y_i, f(x_i)) + \lambda \|f\|_{\mathcal{H}}^2
\]

其中 \( L \) 是损失函数，\( \lambda > 0 \) 是正则化参数。最优解 \( f^*\) 具有形式：
\[
f^* = \sum_{i=1}^n \alpha_i^* k(\cdot, x_i)
\]

**证明思路**: 将 \( f \) 分解为 \( f = f_\parallel + f_\perp \)，其中 \( f_\parallel \) 在 \( \text{span}\{k(\cdot, x_i)\} \) 中，\( f_\perp \) 与之正交。可以证明 \( f_\perp = 0 \)。

### 4.2 应用

**应用**: Representer定理将无限维优化问题转化为有限维问题（\( n \) 个参数）。

---

## 5. 支持向量机 (SVM)

### 5.1 线性SVM

**问题**: 寻找最大间隔超平面：
\[
\min_{w, b} \frac{1}{2}\|w\|^2 \quad \text{s.t.} \quad y_i(\langle w, x_i \rangle + b) \geq 1
\]

### 5.2 核SVM

**对偶问题**:
\[
\max_{\alpha} \sum_{i=1}^n \alpha_i - \frac{1}{2}\sum_{i,j=1}^n \alpha_i \alpha_j y_i y_j k(x_i, x_j)
\]

**约束**: \( \sum_{i=1}^n \alpha_i y_i = 0 \)，\( 0 \leq \alpha_i \leq C \)

**决策函数**:
\[
f(x) = \sum_{i=1}^n \alpha_i^*y_i k(x_i, x) + b^*
\]

### 5.3 对偶问题

**KKT条件**:

- \( \alpha_i = 0 \): 非支持向量
- \( 0 < \alpha_i < C \): 支持向量在边界上
- \( \alpha_i = C \): 支持向量在边界内（软间隔）

---

## 6. 核岭回归

### 6.1 核岭回归

**问题**:
\[
\min_{f \in \mathcal{H}} \sum_{i=1}^n (y_i - f(x_i))^2 + \lambda \|f\|_{\mathcal{H}}^2
\]

**解**（由Representer定理）:
\[
f^*= \sum_{i=1}^n \alpha_i^* k(\cdot, x_i)
\]

其中 \( \alpha^* = (K + \lambda I)^{-1} y \)，\( K \) 是Gram矩阵。

### 6.2 正则化路径

**正则化路径**: 随着 \( \lambda \) 变化，解的变化轨迹。

**性质**:

- \( \lambda \to 0 \): 过拟合风险
- \( \lambda \to \infty \): 欠拟合（趋向常数函数）

---

## 7. 高斯过程

### 7.1 高斯过程定义

**定义 7.1** (高斯过程)
**高斯过程 (GP)** 是随机过程的集合 \( \{f(x): x \in \mathcal{X}\} \)，使得对任意有限集合 \( \{x_1, \ldots, x_n\} \)，\( (f(x_1), \ldots, f(x_n)) \) 服从多元高斯分布。

**均值函数**: \( m(x) = \mathbb{E}[f(x)] \)

**协方差函数（核函数）**: \( k(x, x') = \text{Cov}(f(x), f(x')) \)

### 7.2 核函数选择

**常用核函数**:

- RBF核: 平滑函数
- Matérn核: 控制平滑度
- 周期核: 周期函数

### 7.3 预测与不确定性

**预测分布**: 给定训练数据 \( (X, y) \)，新点 \( x_*\) 的预测分布为：
\[
f(x_*) | X, y \sim \mathcal{N}(\mu_*, \sigma_*^2)
\]

其中：
\[
\mu_* = k_*^T (K + \sigma^2 I)^{-1} y
\]
\[
\sigma_*^2 = k(x_*, x_*) - k_*^T (K + \sigma^2 I)^{-1} k_*
\]

---

## 8. 核方法在深度学习中的应用

### 8.1 神经切线核 (NTK)

**定义 8.1** (神经切线核)
对于神经网络 \( f(x; \theta) \)，**神经切线核 (NTK)** 定义为：
\[
\Theta(x, x') = \left\langle \frac{\partial f(x; \theta)}{\partial \theta}, \frac{\partial f(x'; \theta)}{\partial \theta} \right\rangle
\]

**性质**: 在无限宽极限下，NTK描述神经网络的训练动态。

### 8.2 核方法与深度学习的联系

**联系**:

- 深度网络可以看作学习特征映射
- NTK提供理论分析工具
- 核方法提供函数空间视角

---

## 9. 形式化定义 (Lean)

```lean
-- 正定核
def positive_definite_kernel (k : X → X → ℝ) : Prop :=
  ∀ n : ℕ, ∀ xs : Fin n → X,
    ∀ c : Fin n → ℝ, 0 ≤ ∑ i j, c i * c j * k (xs i) (xs j)

-- RKHS
structure RKHS (X : Type) where
  space : Type
  inner : space → space → ℝ
  kernel : X → X → ℝ
  reproducing : ∀ f : space, ∀ x : X, f x = inner f (kernel · x)

-- Representer定理
theorem representer_theorem (k : X → X → ℝ) (L : ℝ → ℝ → ℝ) (λ : ℝ) :
  ∃ α : Fin n → ℝ,
    optimal_solution = ∑ i, α i * k (·, x_i)
```

---

## 10. 习题

### 基础习题

1. **核函数验证**:
   证明RBF核是正定核。

2. **特征映射**:
   对于二次多项式核 \( k(x, x') = (\langle x, x' \rangle)^2 \)，显式构造特征映射。

3. **Representer定理**:
   使用Representer定理推导核岭回归的解。

### 进阶习题

1. **RKHS构造**:
   给定正定核 \( k \)，构造对应的RKHS。

2. **SVM对偶**:
   推导核SVM的对偶问题。

3. **高斯过程预测**:
   推导高斯过程的预测分布。

---

## 11. 参考资料

### 教材

1. **Schölkopf, B. & Smola, A. J.** *Learning with Kernels: Support Vector Machines, Regularization, Optimization, and Beyond*. MIT Press, 2002.
2. **Rasmussen, C. E. & Williams, C. K. I.** *Gaussian Processes for Machine Learning*. MIT Press, 2006.
3. **Hofmann, T. et al.** "Kernel Methods in Machine Learning." *Annals of Statistics*, 2008.

### 课程

1. **Stanford CS229** - Machine Learning
2. **MIT 6.867** - Machine Learning

### 论文

1. **Jacot, A. et al.** "Neural Tangent Kernel: Convergence and Generalization in Neural Networks." *NeurIPS*, 2018.
2. **Aronszajn, N.** "Theory of Reproducing Kernels." *Transactions of the American Mathematical Society*, 1950.

---

**最后更新**: 2025-12-20
**完成度**: 约75% (核心内容完成，可继续补充更多应用实例和形式化证明)
